#include <iostream>
using namespace std;
int mul = 9; // mul is a globel variable(it work in all code).
int main()
{
    int sum = 6; // sum is a local variable(it work only in {}).
    //******************Data Type*********************

    // 1. Primary Datatype
    int a = 6;           // int stores integers type data.
    float b = 5.6;       // float stores decimal type data.
    double c = 7.787867; // double stores bigger decimal values then float.
    char d = 'T';        // char stores characters.

    // 2. Derived Datatype = Function, Array, Pointer.
    // 3. User Defined Datatype = Union, Enum, Typedef.

    return 0;
}